{
  "name": "lido-occultix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "network": "npx hardhat node",
    "compile": "npx hardhat compile",
    "demo": "npx hardhat run --network localhost --no-compile ./scripts/deployDemo.js",
    "deploy": "npx hardhat run --network localhost --no-compile ./scripts/deployOnly.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-vyper": "^2.0.1",
    "ethers": "^5.2.0",
    "hardhat": "^2.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.2.0"
  }
}
